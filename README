A simple GUI for screen recording with ffmpeg on Linux systems.

To use this you will need the following packages:
	- ffmpeg
	- x11-utils

These can be installed by running

sudo apt-get install ffmpeg x11-utils

from a terminal